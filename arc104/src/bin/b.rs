use proconio::input;
fn main() {
    
    input!{
        n: usize,
        s: String
    }

    fn add_to_tuple(t: (i32,i32,i32,i32), ch: char) -> (i32,i32,i32,i32) {
        match c {
            'A' => 
        }
    }

    let scanned = s.chars().scan((0,0,0,0), |sum_tuple, ch| match ch {
        'A' => su
    })
}
